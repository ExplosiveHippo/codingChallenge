import React, { Component } from 'react';

class SideNavGroup extends Component {

	const menuItems = Data.forEach(menuNode => {
		console.log(menuNode);
	});

	render() {
		return (
			{menuItems}
		);
	}
}

export default SideNavGroup;